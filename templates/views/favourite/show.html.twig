{% extends 'base.html.twig' %}

{% block title %}Favourite{% endblock %}

{% block body %}
    <h1>Favourite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ favourite.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ favourite.email }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ favourite.createdAt ? favourite.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_favourite_index') }}">back to list</a>

    <a href="{{ path('app_favourite_edit', {'id': favourite.id}) }}">edit</a>

    {{ include('favourite/_delete_form.html.twig') }}
{% endblock %}
