{% extends 'layouts/dashboard.html.twig' %}

{% block title %}
    Edit Property
{% endblock %}

{% block body %}
    <h1>
        Modifier un bien
    </h1>
    <div class="content-header row mb-2">
        <div class="">
            <a href="{{ path('app_property_index') }}" class="btn btn-sm btn-light mb-1">
                Retour                       
            </a>
        </div>
    </div>
    <div class="card">
        <div class="card-body">
            {{ include('views/property/_form.html.twig', {'button_label': 'Modifier'}) }}
            {# {{ include('views/property/_delete_form.html.twig') }} #}
        </div>
    </div>

{% endblock %}
