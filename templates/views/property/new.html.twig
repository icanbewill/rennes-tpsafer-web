{% extends 'layouts/dashboard.html.twig' %}

{% block title %}
    Nouveau bien
{% endblock %}

{% block body %}
    <div class="content-header row mb-2">
        <div class="">
            <a href="{{ path('app_property_index') }}" class="btn btn-light mb-1">
                Retour            
            </a>
        </div>
    </div>
    <div class="card">
        <div class="card-body">
            {{ include('views/property/_form.html.twig') }}
        </div>
    </div>
{% endblock %}
