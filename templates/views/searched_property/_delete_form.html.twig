<form method="post" action="{{ path('app_searched_property_delete', {'id': searched_property.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ searched_property.id) }}">
    <button class="btn btn-sm btn-danger">Supprimer</button>
</form>
